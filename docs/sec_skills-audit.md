# `sec_skills` 本地静态审计记录

- 审计日期：2026-08-08
- 来源：`/Users/swyiic/Desktop/Tools/Skills/sec_skills`
- 用途：公司内部、已授权资产自查；不用于未授权目标
- 审计方式：只读静态扫描；未执行其中脚本、命令、Payload、下载或网络请求
- 扫描文件：56 个文本文件，0 个跳过
- 文件总大小：650,471 bytes
- 文件清单 SHA-256：`ca6c889aad04d3a3a22bfa15ee9ade5677af6a363b4bebbe32f9eb9a4d73ec9f`

## 结果

- critical：0
- high：7
- medium：23
- low：2

High 提示集中在参考文档中的示例文本：SSH 私钥路径、`authorized_keys`、编码执行、反弹 Shell 等。它们是用于说明风险或测试场景的文字，不会在导入阶段执行；导入后的 Skill 仍需由 Strix 按授权范围、证据门槛和停止条件运行。

Medium 提示主要是公开文档中的下载、上传、元数据服务、Webhook 和测试命令示例。当前导入逻辑保留完整原文以满足内部方法库迁移需求，但没有自动运行这些内容，也没有自动安装依赖或向外发送数据。

## 导入边界

Oviraptor 将目录中的非隐藏文本文件按相对路径完整存入 `strix_skills`，并记录内容哈希；原目录不会被改写。导入本身只进行读取和数据库写入。实际扫描仍要求目标在授权范围内，普通探测、版本匹配、无复现 CVE 和无影响观察不能直接沉淀为 Skill。

## 后续复核建议

如果将该 Skill 包分享给外部环境，应先把包含真实凭据路径、外传、持久化、提权或破坏性命令的示例拆出，或改成仅描述验证目标、证据和停止条件的版本；本记录不替代人工审查，也不证明运行时行为绝对安全。
